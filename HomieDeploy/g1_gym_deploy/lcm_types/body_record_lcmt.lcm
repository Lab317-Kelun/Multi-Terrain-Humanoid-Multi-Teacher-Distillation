/**
 * 身体记录 LCM 消息定义
 * 通道：未使用（当前代码中未发布或订阅）
 * 
 * 功能：记录身体关节位置（27个关节）
 * 注意：此消息类型在当前代码中未使用，可能是预留或历史遗留
 */
struct body_record_lcmt 
{
    float q[27];               // 关节位置（单位：弧度）
                               // 索引顺序：左腿(0-5) + 右腿(6-11) + 腰部(12-14) + 左臂(15-20) + 右臂(21-26)
                               // 注意：与 body_control_data_lcmt 的 q[29] 不同，这里是 27 个关节
}
