# BEAMDOJO + AMP 训练启动指南

## ✅ 已完成的准备工作

1. ✅ 环境代码 (`humanoid_robot_amp.py`)
2. ✅ 模型代码 (`actor_critic_amp.py`)
3. ✅ 算法代码 (`ppo_double_reward_amp.py`) - **已修复bug**
4. ✅ Storage扩展 (`rollout_storage.py`)
5. ✅ Runner适配 (`on_policy_runner.py`)
6. ✅ 配置文件 (`humanoid_beamdojo_amp_config.py`)
7. ✅ Motion Library (`motion_lib.py`) - **已实现**
8. ✅ 环境注册
9. ✅ Motion数据 (`/home/cft/kelun/Humanoid-Terrain-Bench/legged_gym/data/g1_walk.pkl`)

---

## 🚀 启动训练 - 三步走

### 第1步：验证环境

```bash
cd /home/cft/kelun/Humanoid-Terrain-Bench/legged_gym
conda activate beamdojo_kelun

# 测试环境创建
python3 << 'EOF'
from legged_gym.envs import *
import torch

print("=" * 60)
print("测试1: 创建环境")
print("=" * 60)

env, env_cfg = task_registry.make_env(name="humanoid_beamdojo_amp", args=None)
print(f"✓ 环境创建成功")
print(f"✓ disc_obs_size: {env.disc_obs_size}")
print(f"✓ num_obs: {env.num_obs}")
print(f"✓ num_actions: {env.num_actions}")
print()

print("=" * 60)
print("测试2: 环境Step")
print("=" * 60)

actions = torch.zeros(env.num_envs, env.num_actions, device=env.device)
obs, priv_obs, rewards, dones, extras = env.step(actions)
print(f"✓ Step成功")
print(f"✓ disc_obs在extras: {'disc_obs' in extras}")

if 'disc_obs' in extras:
    print(f"✓ disc_obs shape: {extras['disc_obs'].shape}")
    print(f"✓ disc_obs range: [{extras['disc_obs'].min():.3f}, {extras['disc_obs'].max():.3f}]")
print()

print("=" * 60)
print("测试3: Motion Library")
print("=" * 60)

if hasattr(env, '_motion_lib') and env._motion_lib is not None:
    print(f"✓ Motion Library已加载")
    print(f"✓ 帧数: {env._motion_lib.num_frames}")
    print(f"✓ FPS: {env._motion_lib.fps}")
    
    # 测试采样
    disc_obs_demo = env.fetch_disc_obs_demo(10)
    print(f"✓ 采样demo成功，shape: {disc_obs_demo.shape}")
else:
    print("⚠ Motion Library未加载，将使用随机采样")

print()
print("=" * 60)
print("✅ 所有测试通过！可以开始训练")
print("=" * 60)

EOF
```

**预期输出**:
```
✓ 环境创建成功
✓ disc_obs_size: 820
✓ num_obs: 1082
✓ num_actions: 12
✓ Step成功
✓ disc_obs在extras: True
✓ disc_obs shape: torch.Size([2048, 820])
✓ Motion Library已加载
✓ 帧数: 125
✓ FPS: 120
✓ 采样demo成功，shape: torch.Size([10, 820])
✅ 所有测试通过！可以开始训练
```

---

### 第2步：启动训练（推荐配置）

```bash
cd /home/cft/kelun/Humanoid-Terrain-Bench/legged_gym
conda activate beamdojo_kelun

# 小规模测试训练（快速验证）
python scripts/train.py \
    --task=humanoid_beamdojo_amp \
    --headless \
    --num_envs=512 \
    --max_iterations=100

# 如果测试通过，开始正式训练
python scripts/train.py \
    --task=humanoid_beamdojo_amp \
    --headless \
    --num_envs=2048 \
    --max_iterations=100000
```

**参数说明**:
- `--task=humanoid_beamdojo_amp`: 使用AMP+BeamDojo配置
- `--headless`: 无GUI模式（推荐，速度更快）
- `--num_envs=2048`: 并行环境数量（根据GPU显存调整）
- `--max_iterations=100000`: 最大训练迭代数

---

### 第3步：监控训练

训练启动后，打开WandB查看：https://wandb.ai

**关键指标**:

#### 任务性能
- `Episode_rew/rew_tracking_x_vel`: X方向速度跟踪
- `Episode_rew/rew_foothold`: 落脚点质量
- `Episode_rew/terrain_level`: 地形难度级别
- `Episode_rew/success_rate`: 成功率
- `Train/mean_reward`: 总奖励

#### AMP性能
- `Loss/discriminator`: Discriminator损失（期望0.5-1.0）
- `Loss/discriminator_accuracy`: Discriminator准确率（期望≈0.5）
- `Loss/value_function`: Value loss
- `Loss/surrogate`: Policy loss

#### 训练健康度
- `Policy/mean_noise_std`: 动作噪声标准差
- `Perf/total_fps`: 训练速度（steps/秒）

---

## 📊 预期训练曲线

### 前100次迭代（预热阶段）
- `mean_reward`: 从负值逐渐增加
- `disc_loss`: 可能较大，逐渐稳定到0.5-1.0
- `disc_accuracy`: 从极端值（0或1）逐渐趋向0.5
- `terrain_level`: 保持在0-2级

### 100-1000次迭代（学习阶段）
- `mean_reward`: 持续增加
- `disc_loss`: 稳定在0.5-1.0
- `disc_accuracy`: 稳定在0.4-0.6
- `terrain_level`: 逐渐增加到5-10级

### 1000+次迭代（收敛阶段）
- `mean_reward`: 稳定或缓慢增加
- `disc_loss`: 稳定波动
- `disc_accuracy`: 稳定在≈0.5
- `terrain_level`: 达到最高级别

---

## 🔧 常见问题排查

### Q1: 导入错误

**错误信息**: 
```
ModuleNotFoundError: No module named 'legged_gym'
```

**解决**:
```bash
cd /home/cft/kelun/Humanoid-Terrain-Bench/legged_gym
pip install -e .
```

---

### Q2: Motion Library加载失败

**错误信息**:
```
[AMP] Warning: Motion Library加载失败
```

**检查清单**:
1. 文件是否存在:
   ```bash
   ls -lh data/g1_walk.pkl
   ```

2. 配置文件路径是否正确:
   ```python
   # humanoid_beamdojo_amp_config.py
   amp_motion_files = ['/home/cft/kelun/Humanoid-Terrain-Bench/legged_gym/data/g1_walk.pkl']
   ```

3. 如果仍然失败，查看详细错误:
   ```python
   # 手动测试
   from legged_gym.utils.motion_lib import MotionLib
   motion_lib = MotionLib(
       motion_file='/home/cft/kelun/Humanoid-Terrain-Bench/legged_gym/data/g1_walk.pkl',
       device='cuda',
       num_dofs=27
   )
   ```

---

### Q3: CUDA Out of Memory

**错误信息**:
```
RuntimeError: CUDA out of memory
```

**解决**:
```bash
# 减少并行环境数
python scripts/train.py \
    --task=humanoid_beamdojo_amp \
    --headless \
    --num_envs=1024  # 从2048减少到1024
```

或修改配置文件:
```python
# humanoid_beamdojo_amp_config.py
class env:
    num_envs = 1024  # 改为1024或512
```

---

### Q4: Discriminator准确率异常

**情况A: disc_accuracy > 0.9**

说明Discriminator过拟合，修改配置:
```python
# humanoid_beamdojo_amp_config.py
class algorithm:
    disc_learning_rate = 1e-5    # 从5e-5降低
    disc_loss_weight = 2.0       # 从5.0降低
    disc_grad_penalty = 10.0     # 从5.0增加
```

**情况B: disc_accuracy 一直是0或1**

说明训练不稳定，检查:
1. disc_obs是否有nan: 在训练中添加打印
2. 梯度是否爆炸: 查看WandB的grad_norm

---

### Q5: 奖励不增长

**可能原因**:
1. AMP权重太高，压制了task reward
2. Discriminator学习太快

**解决**:
```python
# humanoid_beamdojo_amp_config.py
class algorithm:
    task_reward_weight = 0.7   # 从0.5增加
    disc_reward_weight = 0.3   # 从0.5减少
```

或暂时禁用AMP:
```python
class env:
    enable_amp = False  # 先只训练task
```

---

## 🎯 训练策略建议

### 策略1：保守启动（推荐新手）

**第1阶段**: 只训练Task（1000次迭代）
```python
# 配置
enable_amp = False
```

**第2阶段**: 加入AMP（从checkpoint继续）
```python
# 配置
enable_amp = True
task_reward_weight = 0.7
disc_reward_weight = 0.3
```

**第3阶段**: 平衡Task和AMP（继续训练）
```python
task_reward_weight = 0.5
disc_reward_weight = 0.5
```

---

### 策略2：直接启动（推荐有经验用户）

```python
# 使用默认配置直接启动
enable_amp = True
task_reward_weight = 0.5
disc_reward_weight = 0.5
```

密切监控前100次迭代，如果出现问题立即调整。

---

### 策略3：激进策略（追求自然运动）

```python
enable_amp = True
task_reward_weight = 0.3
disc_reward_weight = 0.7
disc_learning_rate = 1e-4  # 提高discriminator学习率
```

适合已有较好task policy，想重点优化运动风格的情况。

---

## 📝 训练检查清单

### 启动前
- [ ] conda环境已激活 (`beamdojo_kelun`)
- [ ] legged_gym已安装 (`pip install -e .`)
- [ ] motion文件存在 (`data/g1_walk.pkl`)
- [ ] 配置文件路径正确
- [ ] GPU显存足够（至少8GB）
- [ ] WandB已登录

### 训练中（每100次迭代检查）
- [ ] `mean_reward` 在增加
- [ ] `disc_accuracy` 在0.4-0.6范围
- [ ] `disc_loss` 在0.5-1.0范围
- [ ] 无CUDA OOM错误
- [ ] FPS稳定（>1000）

### 训练后
- [ ] 保存checkpoint (`logs/`目录)
- [ ] 记录最佳性能指标
- [ ] 备份WandB数据

---

## 🎮 可视化训练结果

```bash
# 加载训练好的模型并可视化
python scripts/play.py \
    --task=humanoid_beamdojo_amp \
    --load_run=/path/to/run/folder \
    --checkpoint=10000
```

---

## 📞 获取帮助

如遇到问题:
1. 检查 `/home/cft/kelun/Humanoid-Terrain-Bench/BEAMDOJO_AMP_完整集成文档.md`
2. 查看WandB训练曲线
3. 检查日志文件 `logs/`

---

## 🚀 现在就开始！

```bash
# 复制这个命令直接启动
cd /home/cft/kelun/Humanoid-Terrain-Bench/legged_gym && \
conda activate beamdojo_kelun && \
python scripts/train.py \
    --task=humanoid_beamdojo_amp \
    --headless \
    --num_envs=2048 \
    --max_iterations=100000
```

**祝训练顺利！** 🎉

